import PostPreview from './post-preview'
import Post from '../types/post'

type Props = {}

const TestComponent = () => {
  return (
    <section>
      <div>
        HELLO TEST REACT
      </div>
    </section>
  )
}

export default TestComponent
